# 阶段 2.5：学期周次（weeks 启用）· 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 启用 courses.weeks：用户通过聊天设置学期（开始日期 + 总周数），"今天有什么课"按**当前第几周**过滤课程（支持 "1-16"、"1-8,10-16"、单周/双周语法），并处理未开学/假期/未设置学期三种状态。

**Architecture:** 沿用既有模式：`semester` 单行表存学期配置；新抽取类型 `semester_setting`（独立 record `ExtractedSemesterSetting`，照 Task 6.5 的 `ExtractedOverride` 模式）；`ItemRepository` 增加 `setSemester/semester/weekOf`，`coursesOn` 增加周次过滤；问答上下文的"今日课程"段标注第几周/假期状态。

**Tech Stack:** 不变（Spring Boot 3.5、MyBatis-Plus、SQLite、JUnit 5）。本阶段代码全部基于阶段 2 完成后的 MyBatis-Plus 持久层。

## 执行纪律

- 概念先行 + 费曼复述（同前）；下方代码为导师参考实现。
- TDD：先写测试看红，再实现看绿；每个任务结束提交 + 记笔记。
- 学员已熟练 record/校验模式，`ExtractedSemesterSetting` 建议学员仿照 `ExtractedOverride` 自己先写，导师核对。

## 文件结构总览

```
修改:
├─ src/main/resources/schema.sql               +semester 表
├─ src/main/java/com/campus/agent/store/Database.java   SCHEMA +semester（CLI 路径同步）
├─ src/main/java/com/campus/agent/store/entity/Semester.java        新实体
├─ src/main/java/com/campus/agent/store/mapper/SemesterMapper.java  新 Mapper
├─ src/main/java/com/campus/agent/store/ItemRepository.java  +setSemester/semester/weekOf；coursesOn 加周次过滤
├─ src/main/java/com/campus/agent/store/CourseOccurrence.java  不变（如需要可加 week 字段，建议不加）
├─ src/main/java/com/campus/agent/model/ExtractedItem.java   VALID_TYPES +"semester_setting"
├─ src/main/java/com/campus/agent/model/ExtractedSemesterSetting.java  新 record
├─ src/main/java/com/campus/agent/Prompts.java   EXTRACT 增加 semester_setting 类型与字段
├─ src/main/java/com/campus/agent/service/AssistantService.java   摄入分支 + 今日课程段周次标注
测试:
├─ src/test/java/com/campus/agent/store/SemesterTest.java            新
├─ src/test/java/com/campus/agent/store/CourseScheduleTest.java      增补周次场景
├─ src/test/java/com/campus/agent/model/ExtractedSemesterSettingTest.java  新
├─ src/test/java/com/campus/agent/service/AssistantServiceTest.java  增补
```

---

## Task A: semester 表 + 仓库读写 + 周次计算

**Files:** schema.sql、Database.java、entity/Semester.java、mapper/SemesterMapper.java、ItemRepository.java、store/SemesterTest.java

- [ ] **Step 1: 讲概念**
  - **单行配置表**：应用级的"全局设置"（一学期一条）存数据库而不是写死代码——代码是逻辑，数据是事实，事实要能改
  - **周次计算**：`(今天 - 学期开始) / 7 + 1`；边界：今天早于开始 → 第 0 周（未开学）；超过总周数 → 假期
  - **INSERT OR REPLACE**：SQLite 的 upsert（有则覆盖、无则插入），正好配单行表 id=1

- [ ] **Step 2: 写失败测试** `store/SemesterTest.java`

```java
package com.campus.agent.store;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;

import java.nio.file.Files;
import java.nio.file.Path;
import java.time.LocalDate;
import java.util.Optional;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
class SemesterTest {

    private static Path dbPath;

    @DynamicPropertySource
    static void props(DynamicPropertyRegistry registry) throws Exception {
        dbPath = Files.createTempFile("semester-test", ".db");
        Files.deleteIfExists(dbPath);
        registry.add("spring.datasource.url", () -> "jdbc:sqlite:" + dbPath);
    }

    @Autowired
    ItemRepository repo;

    @Test
    void setAndReadSemesterRoundTrip() {
        repo.setSemester(LocalDate.of(2026, 9, 1), 16);
        Optional<SemesterInfo> s = repo.semester();
        assertTrue(s.isPresent());
        assertEquals(LocalDate.of(2026, 9, 1), s.get().start());
        assertEquals(16, s.get().totalWeeks());
    }

    @Test
    void setSemesterTwiceOverwrites() {
        repo.setSemester(LocalDate.of(2026, 9, 1), 16);
        repo.setSemester(LocalDate.of(2027, 3, 1), 18);
        assertEquals(18, repo.semester().orElseThrow().totalWeeks());
    }

    @Test
    void weekOfBoundaries() {
        repo.setSemester(LocalDate.of(2026, 9, 1), 16);
        assertEquals(0, repo.weekOf(LocalDate.of(2026, 8, 31)).orElseThrow(), "开学前一天为 0");
        assertEquals(1, repo.weekOf(LocalDate.of(2026, 9, 1)).orElseThrow(), "开学当天第 1 周");
        assertEquals(2, repo.weekOf(LocalDate.of(2026, 9, 8)).orElseThrow(), "第 8 天第 2 周");
        assertEquals(16, repo.weekOf(LocalDate.of(2026, 12, 15)).orElseThrow(), "最后一周");
        assertEquals(17, repo.weekOf(LocalDate.of(2026, 12, 22)).orElseThrow(), "超过总周数→假期");
    }

    @Test
    void weekOfUnsetSemesterReturnsMinusOne() {
        // 新临时库 + 未设置 → -1
        assertEquals(-1, repo.weekOf(LocalDate.now()).orElse(-2));
    }
}
```

（注：单行表跨测试共享状态——测试间可能互相干扰。`weekOfUnsetSemesterReturnsMinusOne` 需要学期为空的前置：实现时给测试类加 `@BeforeEach void reset() { repo.clearSemester(); }`，其余测试在方法内自行 setSemester。计划提供 `clearSemester`（DELETE FROM semester）作为测试辅助。）

Run: `mvn -q -Dtest=SemesterTest test` → 红。

- [ ] **Step 3: schema + 实体 + Mapper + 仓库实现**

schema.sql 追加（Database.SCHEMA 同步追加）：

```sql
CREATE TABLE IF NOT EXISTS semester(
  id INTEGER PRIMARY KEY,
  start_date TEXT NOT NULL,
  total_weeks INTEGER NOT NULL,
  note TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')));
```

`SemesterInfo`（store 包，读模型）：`public record SemesterInfo(LocalDate start, int totalWeeks) {}`

`Semester` 实体（entity 包）：@TableName("semester")，字段 id(Long)、startDate(String)、totalWeeks(Integer)、note(String)。

`SemesterMapper extends BaseMapper<Semester>`（@Mapper）。

ItemRepository 增加（注入 SemesterMapper）：

```java
/** 设置学期（单行表 id=1，有则覆盖）。 */
public void setSemester(LocalDate start, int totalWeeks) {
    Semester s = new Semester();
    s.setId(1L);
    s.setStartDate(start.toString());
    s.setTotalWeeks(totalWeeks);
    semesterMapper.insert(s); // 主键冲突时 MyBatis-Plus 默认抛异常
    // → 改为：先 deleteById(1L) 再 insert，或让 Mapper 提供 replace；
    // 推荐：SemesterMapper 增加 @Insert("INSERT OR REPLACE INTO semester(id,start_date,total_weeks) VALUES(1,#{startDate},#{totalWeeks})") 的自定义方法
}

public Optional<SemesterInfo> semester() {
    Semester s = semesterMapper.selectById(1L);
    if (s == null) return Optional.empty();
    return Optional.of(new SemesterInfo(LocalDate.parse(s.getStartDate()), s.getTotalWeeks()));
}

/** 今天是本学期第几周：-1=未设置学期；0=未开学；1..totalWeeks=学期内；>totalWeeks=假期。 */
public Optional<Integer> weekOf(LocalDate today) {
    return semester().map(s -> {
        long days = java.time.temporal.ChronoUnit.DAYS.between(s.start(), today);
        if (days < 0) return 0;
        return (int) (days / 7) + 1;
    });
}

/** 仅测试用：清空学期设置。 */
public void clearSemester() {
    semesterMapper.deleteById(1L);
}
```

（导师提示点：`INSERT OR REPLACE` 与先删后插的区别；主键冲突异常是什么；为什么单行表用固定 id=1。）

- [ ] **Step 4: 验证**：SemesterTest 全绿（解决顺序依赖问题后）；全量 `mvn -q test` 绿。Commit：`feat: semester table with set/read and weekOf computation`

---

## Task B: weeks 解析 + coursesOn 周次过滤

**Files:** ItemRepository.java（coursesOn 过滤）、CourseScheduleTest.java（增补）、（解析器建议独立 `store/WeekRanges.java` 便于单测）

- [ ] **Step 1: 讲概念**
  - **周次表达语法**：`"1-16"`（连续）、`"1-8,10-16"`（多段）、`"5"`（单周）、`"1-16单"`（只单数周，如 1、3、5…）、`"1-16双"`（只双数周）
  - **过滤规则**：weeks 为空 → 全程上课；有值 → 当前周落在任一区间才上课
  - **三种状态**：学期未设置 → 不过滤（保持旧行为）；未开学/假期 → 没有默认课程

- [ ] **Step 2: 写失败测试**（CourseScheduleTest 增补 + WeekRangesTest）

```java
// WeekRangesTest（store 包）
@Test void parseAndMatchRanges() {
    assertTrue(WeekRanges.contains("1-16", 3));
    assertFalse(WeekRanges.contains("1-16", 17));
    assertTrue(WeekRanges.contains("1-8,10-16", 10));
    assertFalse(WeekRanges.contains("1-8,10-16", 9));
    assertTrue(WeekRanges.contains("5", 5));
    assertFalse(WeekRanges.contains("5", 6));
    assertTrue(WeekRanges.contains("1-16单", 3));
    assertFalse(WeekRanges.contains("1-16单", 4));
    assertTrue(WeekRanges.contains("1-16双", 4));
    assertFalse(WeekRanges.contains("1-16双", 3));
    assertTrue(WeekRanges.contains("", 3), "空=全程");
    assertTrue(WeekRanges.contains(null, 3), "null=全程");
}

// CourseScheduleTest 增补
@Test void weeksFilterByCurrentWeek(@TempDir Path tmp) throws Exception {
    // 学期 2026-08-31 开始 16 周；2026-08-31 是周一
    try (Database db = new Database(tmp.resolve("w.db"))) {
        ItemRepository repo = new ItemRepository(db);
        repo.setSemester(LocalDate.of(2026, 8, 31), 16);
        long a = repo.insert(new ExtractedItem("course", "高数", null, null, null,
                "A201", null, null, "08:00", "09:40", 1, "1-8"), 1);
        long b = repo.insert(new ExtractedItem("course", "英语", null, null, null,
                "B101", null, null, "10:00", "11:40", 1, "10-16"), 2);
        long c = repo.insert(new ExtractedItem("course", "体育", null, null, null,
                "操场", null, null, "14:00", "15:40", 1, null), 3);
        // 第 1 周（8-31）：高数+体育
        List<CourseOccurrence> w1 = repo.coursesOn(LocalDate.of(2026, 8, 31));
        assertEquals(2, w1.size());
        // 第 10 周（11-2）：英语+体育
        List<CourseOccurrence> w10 = repo.coursesOn(LocalDate.of(2026, 11, 2));
        assertEquals(2, w10.size());
        assertEquals("英语", w10.get(0).title());
        // 第 17 周（12-21）：假期，无课程
        assertTrue(repo.coursesOn(LocalDate.of(2026, 12, 21)).isEmpty());
    }
}

@Test void noSemesterMeansNoWeeksFilter(@TempDir Path tmp) throws Exception {
    try (Database db = new Database(tmp.resolve("w2.db"))) {
        ItemRepository repo = new ItemRepository(db);
        repo.insert(new ExtractedItem("course", "高数", null, null, null,
                "A201", null, null, "08:00", "09:40", 1, "1-8"), 1);
        assertEquals(1, repo.coursesOn(LocalDate.of(2026, 8, 31)).size(), "未设置学期不过滤");
    }
}
```

Run → 红。

- [ ] **Step 3: 写 WeekRanges**

```java
package com.campus.agent.store;

import java.util.ArrayList;
import java.util.List;

/** 解析 "1-16"、"1-8,10-16"、"5"、"1-16单/双" 形式的周次表达式。 */
public final class WeekRanges {

    private WeekRanges() {
    }

    public static boolean contains(String expression, int week) {
        if (expression == null || expression.isBlank()) return true;
        List<int[]> ranges = new ArrayList<>();
        boolean oddOnly = false, evenOnly = false;
        for (String part : expression.split(",")) {
            String p = part.trim();
            if (p.endsWith("单")) { oddOnly = true; p = p.substring(0, p.length() - 1); }
            if (p.endsWith("双")) { evenOnly = true; p = p.substring(0, p.length() - 1); }
            int dash = p.indexOf('-');
            if (dash > 0) {
                ranges.add(new int[]{Integer.parseInt(p.substring(0, dash)), Integer.parseInt(p.substring(dash + 1))});
            } else {
                ranges.add(new int[]{Integer.parseInt(p), Integer.parseInt(p)});
            }
        }
        boolean inRange = ranges.stream().anyMatch(r -> week >= r[0] && week <= r[1]);
        if (oddOnly) return inRange && week % 2 == 1;
        if (evenOnly) return inRange && week % 2 == 0;
        return inRange;
    }
}
```

- [ ] **Step 4: coursesOn 加过滤**（courses 查询需带出 weeks 列；weekday 过滤后加：）

```java
Optional<Integer> weekOpt = weekOf(date);
List<CourseOccurrence> filtered = new ArrayList<>();
for (Map.Entry<Long, CourseOccurrence> e : byId.entrySet()) {
    CourseOccurrence c = e.getValue();
    if (weekOpt.isPresent() && weekOpt.get() >= 1) {
        // 学期内或假期（>totalWeeks）都应用过滤；weeks 匹配才保留
        if (!WeekRanges.contains(courseWeeksMap.get(e.getKey()), weekOpt.get())) continue;
        if (weekOpt.get() > semester().orElseThrow().totalWeeks()) continue; // 假期无默认课程
    }
    filtered.add(c);
}
```

（实现细节：courses 查询 SELECT 增加 weeks，存入一个 `Map<Long,String>` courseWeeks；总周数判断放在过滤前——`week > totalWeeks` 直接返回空列表更清晰。导师带学员理清这段控制流再动笔。）

- [ ] **Step 5: 验证**：WeekRangesTest + CourseScheduleTest 绿；全量 `mvn -q test` 绿。Commit：`feat: weeks range filtering in coursesOn with odd/even support`

---

## Task C: semester_setting 摄入（聊天设置学期）

**Files:** model/ExtractedSemesterSetting.java（新）、model/ExtractedItem.java（VALID_TYPES）、Prompts.java、service/AssistantService.java、model/ExtractedSemesterSettingTest.java、service/AssistantServiceTest.java

- [ ] **Step 1: 讲概念**：复用 Task 6.5 的"独立 record + 类型分支"模式（学员可先自己仿写）；为什么学期设置走"数据表 + 抽取"而不是配置文件——因为"设置"本身是产品能力，应该用嘴说，不该改文件。

- [ ] **Step 2: 写失败测试**（ExtractedSemesterSettingTest）

```java
@Test void parsesValidJson() {
    ExtractedSemesterSetting s = ExtractedSemesterSetting.fromJson(
            "{\"type\":\"semester_setting\",\"startDate\":\"2026-09-01\",\"totalWeeks\":16,\"note\":\"\"}");
    assertEquals("2026-09-01", s.startDate());
    assertEquals(16, s.totalWeeks());
    assertTrue(s.validate().isEmpty());
}
@Test void rejectsBadDate() {
    ExtractedSemesterSetting s = ExtractedSemesterSetting.fromJson(
            "{\"type\":\"semester_setting\",\"startDate\":\"9月1日\",\"totalWeeks\":16,\"note\":\"\"}");
    assertTrue(s.validate().stream().anyMatch(e -> e.contains("startDate")));
}
@Test void rejectsBadWeekCount() {
    ExtractedSemesterSetting s = ExtractedSemesterSetting.fromJson(
            "{\"type\":\"semester_setting\",\"startDate\":\"2026-09-01\",\"totalWeeks\":99,\"note\":\"\"}");
    assertTrue(s.validate().stream().anyMatch(e -> e.contains("totalWeeks")));
}
```

- [ ] **Step 3: 写 ExtractedSemesterSetting**

```java
package com.campus.agent.model;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;

import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

public record ExtractedSemesterSetting(String startDate, Integer totalWeeks, String note) {

    private static final ObjectMapper MAPPER = new ObjectMapper();

    public static ExtractedSemesterSetting fromJson(String json) {
        final JsonNode n;
        try {
            n = MAPPER.readTree(json);
        } catch (IOException e) {
            throw new RuntimeException("学期设置不是合法 JSON: " + e.getMessage());
        }
        return new ExtractedSemesterSetting(
                blankToNull(n.path("startDate").asText("")),
                n.path("totalWeeks").isInt() ? n.path("totalWeeks").asInt() : null,
                blankToNull(n.path("note").asText("")));
    }

    public List<String> validate() {
        List<String> errors = new ArrayList<>();
        if (startDate == null || !startDate.matches("\\d{4}-\\d{2}-\\d{2}"))
            errors.add("startDate 格式应为 yyyy-MM-dd，收到: " + startDate);
        if (totalWeeks == null || totalWeeks < 1 || totalWeeks > 30)
            errors.add("totalWeeks 应为 1-30 的整数，收到: " + totalWeeks);
        return errors;
    }

    private static String blankToNull(String s) {
        return (s == null || s.isBlank()) ? null : s.trim();
    }
}
```

- [ ] **Step 4: Prompts.EXTRACT 增补**

- type 取值增加 `semester_setting(学期设置：如"本学期9月1日开始，共16周")`
- 字段区增加：`startDate: 学期开始日期 yyyy-MM-dd（仅 semester_setting 用）`、`totalWeeks: 学期总周数（整数，仅 semester_setting 用）`、`note: 备注`

- [ ] **Step 5: 摄入分支**（AssistantService.record() 中，仿 course_override 分支）：

```java
if ("semester_setting".equals(item.type())) {
    ExtractedSemesterSetting st = ExtractedSemesterSetting.fromJson(json);
    List<String> errs = st.validate();
    if (errs.isEmpty()) {
        repo.setSemester(LocalDate.parse(st.startDate()), st.totalWeeks());
        String reply = "✅ 已设置学期：" + st.startDate() + " 开始，共 " + st.totalWeeks() + " 周";
        repo.insertMessage("assistant", reply);
        return reply;
    }
    // 校验失败 → 走既有重试/raw_inbox
}
```

（注意：这段分支在既有校验循环**之后**——先让 ExtractedItem 泛化校验通过，再针对类型做专门校验，与 course_override 分支同构。导师带学员对比两处分支，指出"这是同一个模式"。）

- [ ] **Step 6: 补 AssistantServiceTest**：FakeLlm 返回 semester_setting 抽取 → 断言回执含"已设置学期"且 `repo.semester()` 有值。

- [ ] **Step 7: 验证 + Commit**：全量绿。Commit：`feat: semester_setting extraction type to set semester via chat`

---

## Task D: 问答上下文周次标注 + 端到端验证

**Files:** service/AssistantService.java（今日课程段标注）、设计文档、笔记

- [ ] **Step 1: 讲概念**：上下文标注让 LLM 说"今天是第 3 周"而不是只列课；区分四种状态（未设置/未开学/学期内/假期）各给一句引导。

- [ ] **Step 2: 实现**（answer() 与 handleStreaming() 的 todaySection 组装处）：

```java
String weekLine;
Optional<Integer> weekOpt = repo.weekOf(LocalDate.now());
if (weekOpt.isEmpty()) {
    weekLine = "（未设置学期信息，无法按周次过滤课程；说“本学期从X月X日开始，共N周”即可设置）";
} else {
    int w = weekOpt.get();
    int total = repo.semester().orElseThrow().totalWeeks();
    if (w == 0) weekLine = "（尚未开学，学期 " + start + " 开始）";
    else if (w > total) weekLine = "（假期中，本学期共 " + total + " 周已结束）";
    else weekLine = "今天是本学期第 " + w + " 周（共 " + total + " 周）";
}
```

拼入 `【今天(...)的课程…】` 标题行后。

- [ ] **Step 3: 端到端验收**（学员亲手，浏览器）
  1. 聊天里说"本学期从 9 月 1 日开始，共 16 周" → 回执 ✅
  2. 录入门课"高数 周一 8:00-9:40 A201，1-8 周"
  3. 问"今天有什么课" → 回答含"第 N 周"，且按周次过滤正确
  4. 说"高数下周停课" → 当天问答正确、下下周一自动恢复
  5. `mvn -q test` 全量绿
- [ ] **Step 4: 文档 + 笔记**：设计文档补 semester 表（数据模型节）与问答周次说明；新增 `docs/notes/` 阶段 2.5 笔记（周次计算、单双周解析、单行配置表、upsert）。提交：`docs: phase 2.5 semester weeks notes and spec update`

---

## Task E: 按类型+编号删除记录（与纠正同模式）

**Files:** `service/AssistantService.java`（+delete 分支）、`Prompts.java`（CLASSIFY +DELETE 模板）、`store/ItemRepository.java`（+deleteById）、`web/ChatController.java`（无需改）、测试（ItemRepositoryTest、AssistantServiceTest）

- [ ] **Step 1: 讲概念**
  - **与纠正同构**：分类器加 `delete` 意图 → 带记录列表的抽取提示词让 LLM 输出 `{"type","id"}` → 服务端校验 → 删除 → 回执。对照 Task 9 的纠正代码，学员应先指出两处代码的"相同骨架"再动笔
  - **破坏性操作**：删除不可恢复 → 回执必须**带上被删内容**（标题/类型），录错了可凭回执重新录入；v1 不做二次确认（确认流需要记"待确认状态"，违背无状态原则；若误删频发再加）
  - **删除范围**：v1 只删 5 类业务表（assignment/exam/todo/course/event）；course_overrides（停课记录）的删除后续再加（它不在 /api/items 列表里，删了课程后孤儿变动本就会被 coursesOn 忽略，无碍）
  - **为什么两个字段不建 record**：只有 type+id 两个字段、单处使用 → 直接 JsonNode 解析即可；字段变多或被复用再提升为 record（YAGNI 的边界感）

- [ ] **Step 2: 写失败测试**

```java
// AssistantServiceTest 增补
@Test
void deleteFlowRemovesRecord(@TempDir Path tmp) throws Exception {
    FakeLlm llm = new FakeLlm()
            .json("{\"intent\":\"record\"}")
            .json(VALID_ASSIGNMENT)                    // 先录一条
            .json("{\"intent\":\"delete\"}")
            .json("{\"type\":\"assignment\",\"id\":1}"); // 再删它
    try (Database db = new Database(tmp.resolve("d.db"))) {
        AssistantService service = new AssistantService(llm, new ItemRepository(db), db);
        service.handle("高数作业习题5.2，11月20日前交");
        long id = service.repository().allItems().get(0).id();
        String reply = service.handle("删除作业第1条");
        assertTrue(reply.startsWith("🗑️"), "删除回执应以 🗑️ 开头，实际: " + reply);
        assertTrue(reply.contains("习题5.2"), "回执应带上被删内容: " + reply);
        assertTrue(service.repository().getById("assignment", id).isEmpty(), "记录应已被删除");
    }
}

@Test
void deleteMissingIdGivesHint(@TempDir Path tmp) throws Exception {
    FakeLlm llm = new FakeLlm()
            .json("{\"intent\":\"delete\"}")
            .json("{\"type\":\"assignment\",\"id\":99}");
    try (Database db = new Database(tmp.resolve("d2.db"))) {
        AssistantService service = new AssistantService(llm, new ItemRepository(db), db);
        String reply = service.handle("删除作业第99条");
        assertTrue(reply.contains("没找到"), "应提示没找到，实际: " + reply);
    }
}

// ItemRepositoryTest（SpringBootTest 环境）增补
@Test
void deleteByIdRemovesRow() {
    long id = repo.insert(new ExtractedItem("todo", "领材料", null, null, null, null, null, null, null, null, null, null), 1);
    assertTrue(repo.deleteById("todo", id));
    assertTrue(repo.getById("todo", id).isEmpty());
    assertFalse(repo.deleteById("todo", id), "再删一次应返回 false");
}
```

Run → 红。

- [ ] **Step 3: 实现**

Prompts.CLASSIFY 的取值规则增加一行：
```
- delete：删除某条记录（如“删除作业第3条”“把待办第5条删掉”）
```

Prompts 新增（照 CORRECT 的 {records} 占位模式）：

```java
public static final String DELETE = """
        你是校园助手的删除抽取器。下面是数据库现有记录（JSON 数组）：
        {records}
        用户要删除其中一条。请输出 JSON：{"type":"该记录的类型","id":记录的数字id}
        只输出 JSON，不要输出其他任何文字。
        """;
```

ItemRepository 增加：

```java
/** 按类型+id 删除业务记录；返回是否真的删掉了一行。 */
public boolean deleteById(String type, long id) {
    return switch (type) {
        case "assignment" -> assignmentMapper.deleteById(id) == 1;
        case "exam" -> examMapper.deleteById(id) == 1;
        case "todo" -> todoMapper.deleteById(id) == 1;
        case "course" -> courseMapper.deleteById(id) == 1;
        case "event" -> eventMapper.deleteById(id) == 1;
        default -> throw new IllegalArgumentException("未知类型: " + type);
    };
}
```

AssistantService：`handle()` 的 switch 增加 `case "delete" -> delete(input)`；实现：

```java
private static final Set<String> DELETABLE_TYPES =
        Set.of("assignment", "exam", "todo", "course", "event");

private String delete(String input) {
    try {
        String recordsJson = MAPPER.writeValueAsString(
                repo.allItems().stream().map(StoredItem::toMap).toList());
        String prompt = Prompts.DELETE.replace("{records}", recordsJson);
        String json = llm.chatJson(prompt, input);
        JsonNode n = MAPPER.readTree(json);
        String type = n.path("type").asText("");
        long id = n.path("id").asLong(-1);
        if (id < 0 || !DELETABLE_TYPES.contains(type)) {
            String reply = "没听懂要删除哪条，请带上类型和编号，如“删除作业第3条”。";
            repo.insertMessage("assistant", reply);
            return reply;
        }
        Optional<StoredItem> cur = repo.getById(type, id);
        if (cur.isEmpty()) {
            String reply = "没找到编号为 " + id + " 的" + type + " 记录。";
            repo.insertMessage("assistant", reply);
            return reply;
        }
        StoredItem s = cur.get();
        repo.deleteById(type, id);
        String reply = "🗑️ 已删除：" + typeLabel(type) + "《" + s.title() + "》。删除不可恢复，录错了可重新录入。";
        repo.insertMessage("assistant", reply);
        return reply;
    } catch (SQLException e) {
        throw new RuntimeException("删除出错: " + e.getMessage(), e);
    }
}
```

（`typeLabel` 已在纠正分支用过，直接复用。注意：删除操作**不需要**像纠正那样合并字段——拿到 type+id 后先 `getById` 取回标题再删，是为了回执里带上被删内容。）

- [ ] **Step 4: 验证 + Commit**：新增测试绿；全量 `mvn -q test` 绿；浏览器手工：录两条 → "删除作业第2条" → 记录面板确认消失、回执含标题。Commit：`feat: delete records by type and id via chat`

## 阶段 2.5 完成验收清单

- [ ] `mvn test` 全绿
- [ ] 聊天设置学期 → 回执确认
- [ ] "今天有什么课"按当前周次过滤（含 1-16、多段、单双周语法各验一次）
- [ ] 未开学/假期/未设置学期三种状态提示正确
- [ ] 停课/调课仍正常（与 6.5 功能不冲突）
- [ ] **删除功能**：按类型+编号删除成功、回执含被删内容；编错 id 有提示；误删后能凭回执重新录入
- [ ] 学员能复述：周次怎么算；weeks 表达式怎么解析；为什么学期设置走聊天而不是改配置文件；INSERT OR REPLACE 是什么；**删除与纠正为什么是同一个模式**；**破坏性操作为什么回执要带被删内容**
